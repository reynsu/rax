# Required for rax v2 core pipeline
numpy>=1.24
pyyaml>=6.0
jsonschema>=4.0
mapie>=0.9

# Test dependencies
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100

# Optional — only needed for the multi-judge panel + cross-LLM consensus
# Uncomment the ones you want to use.
# anthropic>=0.30
# openai>=1.50
# google-generativeai>=0.7

# Optional — only needed for the deterministic layer's secret scanning
# (gitleaks / trufflehog are CLIs, install via package manager)

# Optional — `semgrep` for custom rule engine. Install:
#   pip install semgrep
# semgrep>=1.150
